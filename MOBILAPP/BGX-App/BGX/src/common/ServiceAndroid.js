import Utils from "./Utils";
import * as Constants from './Constants';

const ServiceAndroid = async () => {
    console.log('Headless JS task was fired!');
    try {
        // setTimeout(() => {
        //     console.log('Headless JS setTimeout');
        // }, 60000);
    } catch (error) {
    }
};
export default ServiceAndroid;
